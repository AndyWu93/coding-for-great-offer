keep面试题
给定一个由0和1组成的数组arr，arr可以迭代，规则如下
1：下一代还是1
0：如果旁边有一个1，下一代变成1，
   如果旁边没有1或者有两个1，下一代还是0
问第M代arr迭代成什么样子
arr.length:10^6
M:10^9

解题：
  看数据量，O(N)，所以来到i位置，通过公式知道当前位置M代是0还是1
  i位置是1，M代还是1
  i位置是0，就要看它左右两边最近的1距离多少，
  如果有一边距离小于M，肯定变成1了
  如果两=边距离小于M，当时距离一样，那还是0，因为在某一代以后，i两边都是1，它自己就无法变成0了
  如何知道i左右两边距离最近的1的距离？
  辅助数组，从左往右遍历一边，收集数据，再从右往左遍历一边收集数据


外企面试题
有一个有序数组nums，现在将nums里的数都复制给arr，且再将nums里的数乘2后复制给arr，这样arr的长度就是nums的两倍
将arr打乱顺序.
给你打乱后的arr,让你恢复成nums

解题：
  先将arr排序，
  arr[n-1],就是最大值max，max一定是nums的最大值乘2得到的，
  先不忙收集，用一张表吧要收集的数放起来了，比如map
  key:要收集的数
  value：这个数有多少个
  继续从右往左遍历arr,
  如果遇到的数是map中没有的,就表明这个数除2就属于nums里的，就在map中留下记录
  如果遇到的数是map中存在的,就表明这个数就是要收集的数，就收集这个数到nums，同时map里去到记录


有一颗二叉树，每一次收集它的叶节点，收集完后，二叉树的叶节点先去掉，剩下的树中再收集叶节点...直到树都收集完了
求每次收集加起来形成的序列

解题：
    拓扑排序
